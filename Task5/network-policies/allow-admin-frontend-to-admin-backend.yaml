apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-admin-frontend-to-admin-backend
  namespace: systems-client-mart
spec:
  podSelector:
    matchLabels:
      role: admin-back-end-role
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              role: admin-front-end-role
      ports:
        - protocol: TCP
          port: 80
